import styled from "styled-components";

export const HeaderDiv = styled.div`
  margin: 3rem 0;
`;

export const Title = styled.h1` 
  position: relative;
  font-size: 4em;
  display: table;
  margin: 2rem auto;
  font-weight: bold;
  cursor: pointer;
  transition: .6s;
  color: #fff;
`;